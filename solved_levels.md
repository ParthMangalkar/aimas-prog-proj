# Solved Levels

Method: `searchclient_cpp_v2` build r43 (current — extends r41's snapshot-based bounded joint A\* fallback chain with two additional early passes: **pass0D** (decoration-prune early, sound-but-incomplete branching reduction that skips Push/Pull on letters with no goal anywhere) and **pass0E** (active-agent reduction for N ≤ 6 with `relaxed_decoration_aware` eligibility, gives EpicfAIl). Also widens blocker-promotion gate from `n_active ≤ 2` to `n_active ≤ 4`.).
Benchmark conventions: `--algorithm=-prioritized --timeout 30 --max-joint-actions 20000 --normalize` against `misc/server.jar`.
Source CSVs: `benchmarks/results/v2-bench-complevels-r41.csv`, `benchmarks/results/v2-bench-complevels-2026-r41.csv` (r43 deltas captured in this file; new r43 CSV not yet regenerated).

## Summary

| Level Folder | Solved | Total |
|---|---:|---:|
| `complevels` | 31 | 47 |
| `complevels_2026` | 48 | 69 |
| **Total** | **79** | **116** |

Additionally on the course-provided starter set `levels/`: **92 / 104** solved at r41 (see `benchmarks/results/v2-bench-levels-r41.csv` / `.md`).

## Per-level results

| # | Level Name | Level Folder | Solved (Yes/No) | Solve Time | Joint Actions Used | Method Used (Best Method Name, A*, etc.) |
|---:|---|---|---|---:|---:|---|
| 1 | `AIMAS7` | `complevels` | Yes | 0.079s | 152 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 2 | `AMC` | `complevels` | No | 2.244s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 3 | `BigSplit` | `complevels` | No | 0.600s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 4 | `Ccropper` | `complevels` | Yes | 0.086s | 97 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 5 | `ClosedAI` | `complevels` | Yes | 0.117s | 489 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 6 | `CphAirprt` | `complevels` | No | 27.681s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 7 | `DASH` | `complevels` | Yes | 0.081s | 191 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 8 | `DECrunchy` | `complevels` | No | 27.120s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 9 | `DatzCrazy` | `complevels` | No | 0.071s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 10 | `EpicfAIl` | `complevels` | Yes | 5.627s | 77 | Snapshot-based bounded joint A\* **pass0E** (r43: ACTIVE-REDUCED early, N ≤ 6 with `relaxed_decoration_aware` eligibility, prune_sat=true, W=5, 4s budget) — blocker promotion widens to `n_active ≤ 4` so N=4 collapses to N_active=3 |
| 11 | `GHandDirt` | `complevels` | Yes | 0.074s | 51 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 12 | `GroupEZ` | `complevels` | No | 0.105s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 13 | `ISO` | `complevels` | Yes | 9.820s | 3525 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 14 | `Lily` | `complevels` | No | 12.520s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 15 | `LiteralAI` | `complevels` | Yes | 0.101s | 799 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 16 | `MADS` | `complevels` | Yes | 0.073s | 47 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 17 | `MAGiC` | `complevels` | Yes | 0.168s | 562 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 18 | `MAchoupo2` | `complevels` | Yes | 0.086s | 45 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 19 | `MArtians` | `complevels` | Yes | 0.427s | 1631 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 20 | `MAybetwo` | `complevels` | Yes | 0.073s | 31 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 21 | `MAzonakis` | `complevels` | Yes | 0.076s | 92 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 22 | `Medibots` | `complevels` | No | 2.890s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 23 | `Minchia` | `complevels` | No | 2.991s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 24 | `NameHere` | `complevels` | No | 1.268s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 25 | `PFarthing` | `complevels` | Yes | 0.088s | 94 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 26 | `PokeNOM` | `complevels` | No | 0.213s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 27 | `QRscammer` | `complevels` | No | 0.153s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 28 | `Spiraling` | `complevels` | Yes | 0.130s | 1785 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 29 | `TBSTANS1` | `complevels` | Yes | 0.156s | 740 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 30 | `TeamAgent` | `complevels` | Yes | 0.456s | 19 | Snapshot-based bounded joint A\* (passes 1..3b, gated divisor-bound heuristic) — fires after all primary variants + extras exhaust |
| 31 | `TheGate` | `complevels` | No | 1.260s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 32 | `TourDeDTU` | `complevels` | Yes | 0.166s | 98 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 33 | `VNN` | `complevels` | Yes | 0.077s | 189 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 34 | `ZOOM` | `complevels` | No | 27.135s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 35 | `crayCray` | `complevels` | Yes | 0.087s | 304 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 36 | `doggy` | `complevels` | Yes | 0.120s | 349 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 37 | `donkeyK` | `complevels` | Yes | 0.123s | 1090 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 38 | `help` | `complevels` | No | 26.606s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 39 | `jAIl` | `complevels` | Yes | 0.082s | 163 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 40 | `merRAM` | `complevels` | Yes | 1.407s | 1841 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 41 | `mosAIc` | `complevels` | No | 0.151s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 42 | `noone` | `complevels` | Yes | 0.073s | 10 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 43 | `pacMAn` | `complevels` | Yes | 10.760s | 725 | Snapshot-based bounded joint A\* pass 1/2 (admissible h_max, satisfied-box prune toggle) — fires after all primary variants exhaust |
| 44 | `pooh` | `complevels` | Yes | 0.080s | 197 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 45 | `rooMbA` | `complevels` | Yes | 0.095s | 528 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 46 | `sigMAS` | `complevels` | Yes | 0.078s | 219 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 47 | `warehouse` | `complevels` | Yes | 0.096s | 307 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 48 | `AIegean` | `complevels_2026` | Yes | 0.095s | 293 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 49 | `Agentix` | `complevels_2026` | Yes | 0.073s | 14 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 50 | `AlBarah` | `complevels_2026` | Yes | 0.079s | 160 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 51 | `AlphaMAS` | `complevels_2026` | Yes | 0.076s | 129 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 52 | `Apdo` | `complevels_2026` | No | 0.254s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 53 | `AssertFun` | `complevels_2026` | Yes | 0.097s | 82 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 54 | `BStar` | `complevels_2026` | Yes | 0.078s | 81 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 55 | `BigForty` | `complevels_2026` | Yes | 0.088s | 160 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 56 | `BoxBender` | `complevels_2026` | No | 1.596s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 57 | `CBSquad` | `complevels_2026` | Yes | 0.109s | 1090 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 58 | `ClauDOom` | `complevels_2026` | No | 20.941s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 59 | `ComMAndos` | `complevels_2026` | Yes | 0.075s | 44 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 60 | `CphAirprt` | `complevels_2026` | No | 27.264s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 61 | `CudBSlvd` | `complevels_2026` | No | 27.330s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 62 | `DASH` | `complevels_2026` | Yes | 0.085s | 191 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 63 | `DayBreak` | `complevels_2026` | No | 27.260s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 64 | `Dolor` | `complevels_2026` | Yes | 1.004s | 169 | Snapshot-based bounded joint A\* passes 4/5 (active-agent reduction) — N_total > 6 with handful of active agents in residual |
| 65 | `Dracarys` | `complevels_2026` | Yes | 0.122s | 280 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 66 | `Eighty` | `complevels_2026` | Yes | 0.073s | 72 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 67 | `GHandDirt` | `complevels_2026` | Yes | 0.076s | 51 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 68 | `GroupWon` | `complevels_2026` | Yes | 11.205s | 85 | Snapshot-based bounded joint A\* passes 4/5 (active-agent reduction, W=3/W=5) — N_total > 6 but residual N_active ≤ 6 |
| 69 | `Indianish` | `complevels_2026` | Yes | 0.116s | 235 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 70 | `KUTitans` | `complevels_2026` | No | 27.451s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 71 | `LaMAtes` | `complevels_2026` | Yes | 0.189s | 1425 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 72 | `LoopBots` | `complevels_2026` | Yes | 22.688s | 320 | Snapshot-based bounded joint A\* pass 3c (divisor-bound heuristic forced, W=3) — multi-box low-h_max residual needs h_sum/N_eff guidance |
| 73 | `LoveLock` | `complevels_2026` | Yes | 0.091s | 384 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 74 | `MAPFlame` | `complevels_2026` | Yes | 0.078s | 109 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 75 | `MASaos` | `complevels_2026` | Yes | 0.312s | 408 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 76 | `MASstroke` | `complevels_2026` | Yes | 0.107s | 323 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 77 | `MAceship` | `complevels_2026` | Yes | 0.075s | 87 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 78 | `MAface` | `complevels_2026` | Yes | 0.117s | 338 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 79 | `MAgic` | `complevels_2026` | Yes | 0.090s | 356 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 80 | `MAmaMASS` | `complevels_2026` | Yes | 0.075s | 45 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 81 | `MArachnid` | `complevels_2026` | Yes | 0.184s | 314 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 82 | `MAuseCat` | `complevels_2026` | Yes | 0.192s | 2395 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 83 | `MAvis` | `complevels_2026` | Yes | 0.102s | 78 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 84 | `MAze` | `complevels_2026` | No | 27.017s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 85 | `MazeRun` | `complevels_2026` | Yes | 0.081s | 348 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 86 | `Minchia` | `complevels_2026` | No | 2.926s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 87 | `Nej` | `complevels_2026` | No | 13.719s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 88 | `NicKiS` | `complevels_2026` | Yes | 0.101s | 422 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 89 | `NineChars` | `complevels_2026` | Yes | 0.074s | 102 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 90 | `OPN` | `complevels_2026` | Yes | 0.089s | 157 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 91 | `OlsenBand` | `complevels_2026` | Yes | 0.087s | 517 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 92 | `PJMAS` | `complevels_2026` | Yes | 0.090s | 233 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 93 | `PinWheel` | `complevels_2026` | No | 26.769s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 94 | `Planarchy` | `complevels_2026` | No | 27.127s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 95 | `ProjectD` | `complevels_2026` | No | 0.195s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 96 | `RaedFathi` | `complevels_2026` | Yes | 0.069s | 4 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 97 | `SeisSiete` | `complevels_2026` | Yes | 0.079s | 169 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 98 | `TheDevil` | `complevels_2026` | Yes | 0.098s | 412 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 99 | `Tittling` | `complevels_2026` | No | 27.109s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 100 | `TriSplit` | `complevels_2026` | No | 22.320s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 101 | `TriWards` | `complevels_2026` | Yes | 0.501s | 640 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 102 | `TwoPlayer` | `complevels_2026` | Yes | 0.074s | 139 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 103 | `WardRush` | `complevels_2026` | No | 14.733s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 104 | `WeTried` | `complevels_2026` | Yes | 0.102s | 701 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 105 | `amogus` | `complevels_2026` | No | 0.081s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 106 | `brAIn` | `complevels_2026` | No | 0.379s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 107 | `donut` | `complevels_2026` | Yes | 0.073s | 119 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 108 | `duckie` | `complevels_2026` | Yes | 0.149s | 196 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 109 | `escAIpe` | `complevels_2026` | No | 0.107s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 110 | `lilchal` | `complevels_2026` | Yes | 0.079s | 281 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 111 | `logo` | `complevels_2026` | Yes | 0.071s | 55 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 112 | `makeMAga` | `complevels_2026` | Yes | 0.193s | 695 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 113 | `moveMAcat` | `complevels_2026` | Yes | 1.303s | 1818 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 114 | `slAIve` | `complevels_2026` | No | 0.725s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 115 | `sublevels` | `complevels_2026` | No | 0.733s |  | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
| 116 | `trauMA` | `complevels_2026` | Yes | 0.094s | 601 | Modular v2 (`-prioritized`, r41): matched-greedy + DP-optimal task variants → defer/scatter/corridor-evac/relocation delivery → PIBT + cooperative A\* + serial + agent-only joint A\* final phase → min-max-DP/shuffle extras → from-initial joint A\* (W=3,W=5) → snapshot-based bounded joint A\* chain (passes 1..5b: gated + force-divisor-bound + active-agent reduction) |
